# Phase 9 — V2.1 Audit (Jury)

**Auditor:** Jury (orchestrator + 6-reviewer lens — Halo / Optic / Proof / Compass / Trace / Canon)
**Date:** 2026-05-12
**Scope:** V2.1 Batch 1 — commits `a6c6832` (Atlas+Comply: `0010_scaffold_v2_1.sql` 557 lines — `scaffold_jobs` + `scaffold_state` enum + 4 `audit_action` extensions + 7 RLS policies + 18 schema deltas; Build extension v1.0 → v1.1 §2A scaffold IP; AI System Card v1.5 → v1.6; V2.1 compliance audit 359 lines) and `a96dbce` (Forge: scaffold generator — `stack-detector.ts` + 3 scaffolders [`nextjs-saas`/`nodejs-worker`/`cli-tool`] + `code-generator.ts` + `audit-gate.ts` + `bundle-zip.ts` + orchestrator `index.ts`; 3 V2.1 customer-facing routes; 5 carry closures — V1.5 VF1 Art. 50 verbatim in pr-opener, VF3 pinned-versions.json, M5 CSP+HSTS, 3 test aliases, AT .webm placeholders; 1,155/26/0 test result). Audited against `sprint/milestone-V2-1.md` exit gate + `PRD.md` §16 V2.1 + §7.3 Build workflow + `BUILD_FLOW.md` Phase 9 + EU AI Act Art. 50 binding 2026-08-02 + the V2 audit's enumerated carries.
**Self-dogfood gate V2.1:** APPLIED (6-reviewer lens on V2.1 new attack surface — generated executable code that customers run in production; the scaffolders are the code Studio Zero would itself produce, audited by Studio Zero's own gate — the most recursive surface in the series).
**Verdict at V2.1 close:** **PASS WITH FIXES — score 80.** Above the 70 PASS threshold; matches V2's 80, +1 over V1.5. The Forge commit closed three of the four V2 carries plus three M5 carries (4 of 4 chronics named in V2.1's carry list landed); however **VF1-V2 (Art. 50 paragraph drift in `bundle-assembler.ts`) and VF2-V2 (Build-mode token cap) are STILL OPEN at V2.1 close** despite the dispatch focus, and three NEW V2.1-specific findings hold the score from breaking V2's mark.

Series shape: **M0 75 · M1 75 · M2 78 · M3 77 · M4 80 · M5 82 · V1.5 79 · V2 80 · V2.1 80.** V2.1 ties V2 — the cleanest closure-of-carries pattern in the series (4 of 7 named V2 + V1.5 + M5 carries closed in a single Forge batch) is offset by the two V2 carries that survived plus three new findings on the scaffold surface.

---

## 1. Per-producer verdict

### 1.1 Atlas + Comply — commit `a6c6832` — **PASS WITH FIXES (Minor + Minor)**

**1,266 net lines across 5 files.** Migration `0010_scaffold_v2_1.sql` (557 lines) introduces the `scaffold_jobs` table (ULID-PK, brief_ref, audit_verdict, generated_artifact_id, refunded_at), `scaffold_state` enum (10 values), 4 new `audit_action` extensions (`scaffold_dispatched`/`scaffold_audit_passed`/`scaffold_audit_failed`/`scaffold_refunded`), 7 RLS policies (mirroring V1.5/V2 patterns — only the scaffold-audit-gate Edge Function may transition state to `audit_passed`/`audit_failed`), CHECK constraint `scaffold_jobs_delivered_verdict_required` (tightened beyond V2's `builds_delivered_verdict_required` by additionally requiring `generated_artifact_id IS NOT NULL` — defense against ghost-delivery rows). Build extension v1.0 → v1.1 §2A inserted between §2 and §3 (preserves all downstream cross-references): §2A.1 scaffold IP, §2A.2 Art. 50 NOTICE-file disclosure, §2A.3 audit-gate hard rule, §2A.4 production-readiness disclaim, §2A.5 customer-review obligation (third human-oversight gate — the V2.1 governance innovation), §2A.6 license+liability, §2A.7 7-day rejection refund (in addition to non-waivable EU/UK 14-day cooling-off), §2A.8 pricing, §2A.9 customer warranties extend, §2A.10 similarity disclosure extends. AI System Card v1.6 (supersedes v1.5; quarterly re-verification 2026-08-12) adds three NEW V2.1 risk rows (R-V2.1-1 undisclosed third-party deps, R-V2.1-2 similar-to-public-references in code, R-V2.1-3 production-readiness expectation mismatch), four NEW V2.1 performance metrics, and the third human-oversight gate. V2.1 compliance audit scores 21 controls — 18 LIVE, 1 RE-VERIFIED, 2 HUMAN-PENDING.

**What Atlas+Comply nailed:**

- **§2A inserted (not renumbered).** The choice to number the new section §2A rather than renumber §3 onward preserves every cross-reference in System Card v1.5 + v1.6 + V2 compliance audit + V2 migration header — a discipline that the M3 / M5 carries' filename-alias pattern revealed the cost of. Atlas chose the lower-cost path. **Cross-ref hygiene best-of-series at V2.1 close.**
- **Third human-oversight gate is honest.** §2A.5 names the customer-review obligation in plain language (dependency manifest, env-var footprint, auth/authz posture, test suite — reviewed before deployment) and the AI System Card v1.6 §8 documents it as the THIRD gate after (1) brief confirmation and (2) bundle review. Deliberately no machine-checked "customer reviewed" tick — Comply named the trap and avoided it. GDPR Art. 22 satisfied: scaffold delivery is **not** a fully-automated decision affecting a data subject.
- **DB-side defense-in-depth tightened.** `scaffold_jobs_delivered_verdict_required` adds `AND generated_artifact_id IS NOT NULL` to the V2 pattern — closes a ghost-delivery class of bug the V2 CHECK doesn't catch.
- **R1 LLM cost amplification correctly framed for Scaffold.** §7 R1 row notes scaffold is an _additional_ layer-lead loop on top of V2 bundle generation; reuses the M2 110% headroom hard-cap. No new technical surface — re-uses the established mechanic.
- **AI System Card v1.6 honesty discipline maintained.** §3.1 still honestly flags `pinned-versions.json` as HUMAN-pending at the time of v1.6 publication, even though the Forge sibling commit `a96dbce` closes it (the System Card was written assuming the carry was open — Comply notes this in §3.1's "status target" line). §1 still flags Art. 27 HUMAN-pending. Performance metrics §9 NEW V2.1 rows are framed as "measured at V2.1+30d" — the card does not claim data it does not have.

**Atlas+Comply Minor findings:**

- **VC1-V2.1 (Minor) — §2A.2 + §8 claim per-scaffold `NOTICE` file LIVE, but Forge's scaffolders do NOT emit one.** Compliance §2.9 + Build extension §2A.2 + System Card v1.6 §8 all declare every delivered scaffold zip ships with a top-level `NOTICE` file carrying the locked-verbatim Art. 50 disclosure. None of the three Forge scaffolders (`nextjs-saas.ts` / `nodejs-worker.ts` / `cli-tool.ts`) emit a `NOTICE` file. The scaffolders include Art. 50 prose inline in README.md, in `app/page.tsx`, and in inline file headers — which is good Art. 50 hygiene — but the explicit `NOTICE` file the legal extension contractually promises is absent from the disk-side output. **Closure (~15 min):** Forge adds a `NOTICE` constant (sharing the same locked paragraph the V1.5 VF1 `art50-disclosures.ts` carries) and each scaffolder pushes `{ path: "NOTICE", contents: NOTICE_ART50_DISCLOSURE, pinned: true }`. Spec asserts the file is present in the zip.
- **VC2-V2.1 (Minor) — V2.1 compliance audit's exit-gate cross-reference cites `tests/integration/scaffold-audit-gate.spec.ts`, but the on-disk file is `tests/integration/scaffold-audit-gate-fail.spec.ts`.** Same filename-alias drift pattern as M5/V1.5/V2 (VF4-V2). Compliance §2.4 + §2.8 evidence rows name the path `tests/integration/scaffold-audit-gate.spec.ts`; CI grep-gate on that exact name will not find it. **Closure (~5 min):** Verify ships a thin filename-alias wrapper at `tests/integration/scaffold-audit-gate.spec.ts` re-exporting from the on-disk file — same pattern as the prior carries.

### 1.2 Forge — commit `a96dbce` — **PASS WITH FIXES (Major + Major + Minor)**

**3,612 net lines across 28 files.** Scaffold generator pipeline under `apps/runner/src/build/scaffold-v2-1/`: `types.ts` (133 lines — `ScaffoldStack` enum + `ScaffoldFile` + `ScaffolderInput`/`Output` + `ScaffoldAuditResult` + `ScaffoldDeliverable` + `ScaffoldOrchestratorDeps` typed contracts), `stack-detector.ts` (162 lines — keyword/team-roster/risk-profile heuristics → `nextjs-saas` / `nodejs-worker` / `cli-tool` with confidence), `code-generator.ts` (144 lines — gateway-mediated brief-sensitive file gen with graceful template-fallback), 3 scaffolders (`nextjs-saas.ts` 457 lines — package.json + tsconfig + next.config.ts with CSP+HSTS + Supabase client + Stripe client + middleware + health/healthz routes + README + .env.example + .gitignore + layout/page; `nodejs-worker.ts` 249 lines; `cli-tool.ts` 199 lines), `audit-gate.ts` (120 lines — `decideVerdict` rule table mirroring PRD §7.2 + `runScaffoldAuditGate` + mock Jury for tests), `bundle-zip.ts` (176 lines — pure-Node PKZIP STORE-only zip with table-driven IEEE-802.3 CRC-32 + deterministic 2026-05-12 12:00:00 DOS timestamp + UTF-8 filenames + no symlinks; deliberately not general-purpose — Cipher lockfile-minimalism rule), orchestrator `index.ts` (288 lines — brief → stack-detect → code-gen → compose → audit-gate → zip with `BuildEvent` emission at every phase). Three V2.1 web routes: `/api/builds/[buildId]/scaffold/generate/route.ts` (196 lines), `/app/builds/[buildId]/scaffold/page.tsx` (179 lines progress dashboard), `/app/builds/[buildId]/scaffold/download/page.tsx` (92 lines download CTA — closes V2 customer-side UI carry). Carry closures: V1.5 VF1 `ART50_DISCLOSURE` constant updated to verbatim `legal/pr-body-template.md` §2 paragraph; `runner/llm/pinned-versions.json` shipped with v1 schema + anthropic primary/fast + openrouter fallback + rotation policy + `pinned-versions.test.ts`; `apps/web/next.config.ts` CSP (10 directives — Stripe + Supabase + PostHog + Sentry) + HSTS preload (1-year + includeSubDomains); 3 M5 named test aliases (`status-page.spec.ts` 128 lines + `retention-purge.spec.ts` 144 lines + `gdpr-right-to-delete.spec.ts` 198 lines as new on-disk spec, not alias); 2 AT-recording `.webm` placeholders for M4 NVDA + VoiceOver flows; `auto-pr-flow.spec.ts` + `auto-pr-art50-disclosure.spec.ts` updated for verbatim Art. 50 paragraph. Two V2.1 integration specs: `scaffold-generation-happy.spec.ts` (190 lines — full pipeline happy path on all 3 stacks + zip contains expected files + zip is a valid PKZIP stream) and `scaffold-audit-gate-fail.spec.ts` (171 lines — Critical finding ⇒ deliverable=false + zip_bytes empty + `failed` event with `refund: true` + `decideVerdict` table covers all 5 cases). Plus `scaffold-clean-vm-bootstrap.spec.ts` (87 lines).

**What Forge nailed:**

- **Recursive audit-gate is the V2.1 architectural win.** `audit-gate.ts:1-19` documents the discipline clearly: the Studio Zero audit panel is the QA gate for Studio Zero's OWN scaffold-generated output. We audit ourselves with the same gate we sell. The `decideVerdict()` rule table is exported and unit-tested independently of the pipeline. The mock Jury (`makeMockJuryAudit()`) uses a `SECURITY_BUG` sentinel as the FAIL trigger — the same kind of deterministic seam V1.5 + V2 used for the audit-gate negative tests. `scaffold-audit-gate-fail.spec.ts` asserts the full FAIL → no-zip → refund-event chain. **This closes the V2.1 milestone exit gate's audit-gated-delivery requirement at the integration level.**
- **VF3 (M3→M4→M5→V1.5→V2 chronic — SIX milestones) — CLOSED.** `apps/runner/src/llm/pinned-versions.json` is on disk with v1 schema, pinned `claude-sonnet-4-6` primary + `claude-haiku-4-5-20251001` fast (the same models the runner has been using all along — the carry was about pinning the contract, not picking the models), openrouter fallback lane (closes V2 VC2 R9 partial), rotation policy with quarterly Forge cadence + 7d-soak rule. `llm-gateway-client.ts:53-83` adds `loadPinnedVersions()` synchronous loader + `resolvePinnedModel(modelClass)` and asserts at module-init time. `RealGatewayClient` constructor invokes `loadPinnedVersions()` so the file's absence/malformation explodes at client construction (not mid-build). `pinned-versions.test.ts` (52 lines) asserts the file's required keys + the model-class mapping. **The six-milestone chronic is closed.** AI System Card §3.1 should be updated to LIVE on the next v1.6.x re-cut.
- **V1.5 VF1 — CLOSED.** `pr-opener.ts:54-57` now carries the locked-verbatim `legal/pr-body-template.md` §2 paragraph as the `ART50_DISCLOSURE` constant — same paragraph Comply locked. `auto-pr-art50-disclosure.spec.ts` (updated) and `auto-pr-flow.spec.ts` exact-string-assert the disclosure. The file header explicitly cites VF1's nature ("V2.1 Batch 1 (Forge) — VF1 carry close") so the commit message + code comment + spec form a complete provenance chain.
- **M5 carries — 4 of 5 CLOSED.** CSP + HSTS shipped in `apps/web/next.config.ts` with documented allowlist + ARCH-D11 cross-ref for the `'unsafe-inline'` concession until Next 16. Three named test aliases shipped (`status-page` / `retention-purge` / `gdpr-right-to-delete`). The AT-recording `.webm` files are present at zero bytes — Forge's signoff.md documents the placeholders explicitly. **One M5 carry — actual AT recording capture by Halo + Vega — is STILL open.** Forge's commit honestly notes this in the signoff.md update.
- **V2 customer-side UI carry — CLOSED.** `/app/builds/[buildId]/scaffold/page.tsx` (progress) and `download/page.tsx` (CTA) close the V2 audit's "richer customer-output UI + zip-download" carry. The progress page subscribes to the `build:<buildId>` Realtime channel (same pattern as V2 dashboard) and renders the 6 scaffold phases (stack_detect / code_gen / compose / audit_gate / bundle_zip / delivered) with live progress.
- **Scaffold IP correctness.** Each scaffolder emits inline Art. 50 disclosure in the README + the landing page + file headers — the disclosure travels with the code into the customer's environment as `build-mode-extension.md` §2A.2 governs. The CSP allowlist in `nextjs-saas.ts:201-211` matches the Studio Zero `next.config.ts` Shield TB-1 posture — the scaffolded customer's code inherits Studio Zero's security floor. (See VF3-V2.1 finding below for the gap.)
- **Pure-Node zip implementation.** `bundle-zip.ts` deliberately avoids JSZip / archiver / yauzl. Per Cipher v0.5 "lockfile-minimalism" rule, the runner dep graph stays tight. The IEEE-802.3 CRC-32 table is computed at module-init; DOS timestamps are fixed-deterministic for reproducible-build hygiene.

**Forge Major Jury-gated findings:**

- **VF1-V2.1 (Major — V2 carry STILL OPEN) — `bundle-assembler.ts` Art. 50 paragraph drift NOT closed despite VF1 close in `pr-opener.ts`.** `apps/runner/src/build/v2/bundle-assembler.ts:158-159` STILL emits the original V2 wrong paragraph: _"This bundle was produced by Studio Zero. See https://studio-zero.app/ai-system-card for the System Card v1.0."_ — wrong domain (`studio-zero.app` vs `studiozero.dev`), wrong System Card version (v1.0 vs v1.6), no Art. 50 binding citation, no GDPR-Art-22-cleared human-oversight language, and STILL only on the README — not on the roadmap/architecture/PRD/brand-tokens/voice/decisions/risks/COGS/channels documents that System Card §8 + Compliance §2.9 explicitly require. **Forge's V2.1 commit fixed the V1.5 `pr-opener.ts` Art. 50 paragraph; it did NOT fix the V2 `bundle-assembler.ts` Art. 50 paragraph the V2 audit explicitly flagged as VF1-V2.** The V2 audit's recommended closure was: _"Forge factors the Comply-locked paragraph into a single `ART50_BUNDLE_DISCLOSURE` constant (parameterized for system-card version stamp), prepends it to every emitted markdown artifact in `bundle-assembler.ts`, and updates `build-mode-happy-path.spec.ts` to exact-string-assert the disclosure on at least three artifacts (README, PRD, voice)."_ — this work is NOT in the V2.1 batch. The V2.1 dispatch closed V1.5 VF1 (in `pr-opener.ts`) but mislabeled this as also closing VF1-V2 (in `bundle-assembler.ts`); the commit message reads "Art. 50 verbatim" without scoping the surface, and the dispatch task description sequenced V1.5 VF1 + VF3 + M5 + tests but did not name VF1-V2 explicitly. **The drift now spans the V2 bundle surface alone (the V1.5 surface is closed) — one emitter wide, but one of the load-bearing ones (every roadmap-bundle artifact a customer downloads carries the wrong disclosure).** **Closure (~30 min):** Forge factors the locked paragraph into `apps/runner/src/legal/art50-disclosures.ts` (or imports from `pr-opener.ts`); `bundle-assembler.ts` prepends to every emitted markdown artifact (README + PRD + roadmap + architecture + voice + decisions + risks + COGS + channels — 9 artifacts); `build-mode-happy-path.spec.ts` exact-string-asserts on at least 3 artifacts. Mark as P0 for V2.1 hotfix or V2.2 dispatch.
- **VF2-V2.1 (Major — V2 carry STILL OPEN) — Build-mode stricter token cap STILL documented in a comment but NOT implemented.** `layer-dispatcher.ts:30-33` still carries the specification comment about the per-tenant Build-mode token cap and the `build_mode: true` gateway flag; `llm-gateway-client.ts` does NOT propagate a `build_mode` flag (the V2.1 code-generator calls `gateway.message()` for scaffold without a build_mode signal); `supabase/functions/llm-gateway/index.ts` has no `build_mode` arm; migration `0010_scaffold_v2_1.sql` does NOT add `tenants.build_token_budget_micros` or `scaffold_token_budget_micros`; no `check_build_token_budget()` / `check_scaffold_token_budget()` SECURITY-DEFINER function. AI System Card v1.6 §7 R1 row claims the mitigation is LIVE and scaled-for-scaffold; Compliance §2.10 + §2.15 mark it LIVE. **The mitigation as documented does not exist in code for V2 OR V2.1** — the cap is the unified R1 110% headroom from M2's `0003_billing_managed.sql`, which is shared between audit + build + scaffold. **Penny's V2.1 unit-economics claim and Compliance §2.10 + §2.15 LIVE status are still overstated; V2.1 amplifies the gap because scaffold dispatch is an _additional_ layer-lead loop.** **Closure (~2 hours):** (a) llm-gateway accepts `build_mode` and `scaffold_mode` request-body flags + picks alternate budget multipliers; (b) `layer-dispatcher.ts` propagates `build_mode: true`; (c) scaffold orchestrator + code-generator propagate `scaffold_mode: true`; (d) Atlas adds `tenants.build_token_budget_micros` + `tenants.scaffold_token_budget_micros` columns OR a SECURITY-DEFINER `check_build_token_budget()` / `check_scaffold_token_budget()` function. Mark as P0 for V2.1 hotfix or V2.2 dispatch.

**Forge Minor findings:**

- **VF3-V2.1 (Minor) — Scaffolded `nextjs-saas` CSP is a STRICT SUBSET of Studio Zero's own CSP (allowlist asymmetry).** `nextjs-saas.ts:201-211` emits a 10-directive CSP for the scaffolded customer's app; Studio Zero's own `apps/web/next.config.ts:30-58` ships a 13-directive CSP that adds: PostHog allowlist (`https://js.posthog.com` + `https://eu.i.posthog.com`), Sentry replay loader (`https://browser.sentry-cdn.com`), and PostHog/Sentry connect-src + frame-ancestors `'none'`. This is intentional (the scaffolded customer doesn't have PostHog/Sentry by default), but **the scaffolded `next.config.ts` does not carry an inline comment explaining _why_ the CSP is a subset** — a customer adding PostHog or Sentry will hit a blank-screen CSP-block with no breadcrumb. **Closure (~10 min):** Forge adds a docblock above `CSP_DIRECTIVES` in the scaffolded `next.config.ts` template explaining the subset choice + linking to Studio Zero's own CSP as a reference. This is a Compass + Herald-relevant scaffold-quality finding (the scaffold inherits the security floor but not the _explanation_ of the security floor).

### 1.3 Producer-pair joint verdict

The Atlas+Comply commit landed first (a6c6832) and the Forge commit landed seven minutes later (a96dbce) — a coordinated two-step batch. Cross-reference hygiene between the two commits is the strongest of the series, BUT three coordination gaps survive:

1. **VC1-V2.1 (NOTICE file)** is a coordination gap: the legal extension and System Card promise a `NOTICE` file; Forge's scaffolders do not write one. The integration plane between legal-as-contract and runner-as-emitter has the same pattern as VF1-V2 in V2 — different surface, same shape.
2. **VF1-V2.1 (bundle-assembler Art. 50)** is the V2 carry that survived V2.1: the dispatch named V1.5 VF1 as a carry to close but did not name VF1-V2; Forge closed the named one and missed the unnamed sibling. The Jury panel should have caught this in dispatch-review.
3. **VF2-V2.1 (token cap)** is the V2 carry that survived V2.1 unchanged: the dispatch did not name VF2-V2 at all, and the V2.1 surface AMPLIFIES the gap (scaffold is an additional layer-lead loop). System Card v1.6 §7 R1 should have been re-framed honestly to PARTIAL — instead it was upgraded to LIVE for V2.1.

The Comply-vs-emitter integration gap pattern is now **three milestones long** (V1.5 VF1 in `pr-opener`, V2 VF1 in `bundle-assembler`, V2.1 VC1 in `NOTICE`-file emission). If it appears again at V2.2 or M3-close, the panel should treat it as a **structural drift** in the legal-runner integration plane and propose an architectural change (a single `art50-disclosures.ts` constants module imported by all emitters; CI grep-gate that asserts every legal-cited surface emits the exact string).

---

## 2. V2.1 exit-gate scorecard (per `sprint/milestone-V2-1.md` L86–L91)

| #   | Exit-gate item                                                                         | Status at HEAD                                                                                                                                                                                                                                                                                                                                  | Closer                                 |
| --- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| 1   | `tests/integration/scaffold-generation-happy.spec.ts` green                            | **GREEN** — 190 lines; full pipeline on 3 stacks; zip-content asserts; PKZIP-shape asserts                                                                                                                                                                                                                                                      | Forge ✓                                |
| 2   | `tests/integration/scaffold-audit-gate-fail.spec.ts` green (audit FAIL halts delivery) | **GREEN** — 171 lines; Critical ⇒ deliverable=false + zip empty + refund event; `decideVerdict` 5-case table                                                                                                                                                                                                                                    | Forge ✓ (VC2-V2.1 filename-alias open) |
| 3   | Roadmap-bundle schema committed; ajv validates                                         | **GREEN at the Forge path** `apps/runner/schemas/roadmap-bundle.v1.schema.json` — but **DIVERGENT** from Atlas's `architecture/schemas/roadmap-bundle.v1.schema.json` (VA1-V2 STILL OPEN at V2.1 — NOT in this batch)                                                                                                                           | V2 carry — Forge + Atlas               |
| 4   | AI-Authored trailer on generated scaffold (carry from V1.5 pattern)                    | **PARTIAL** — Auto-PR commit trailer (V1.5 surface) is live; scaffold zip's NOTICE file is NOT emitted by scaffolders (VC1-V2.1); scaffold seeded-repo commit trailer is documented but no seeded-repo path is wired for V2.1 (zip-only delivery)                                                                                               | Forge (VC1 close)                      |
| 5   | Customer-facing `/app/builds/[id]/scaffold/{progress, download}` routes                | **GREEN** — `scaffold/page.tsx` (progress) + `scaffold/download/page.tsx` shipped; Realtime channel subscribed; CTA wired                                                                                                                                                                                                                       | Forge ✓                                |
| 6   | Clean-VM bootstrap of a generated scaffold completes in <30 min                        | **PARTIAL** — `scaffold-clean-vm-bootstrap.spec.ts` (87 lines) shipped as an integration spec but the actual fresh-Ubuntu/Windows CI runner is Pipeline's deliverable per the V2.1 burndown; the spec asserts the bootstrap shape but does not run on a clean VM at CI time (the Pipeline + Verify clean-VM runner is week-29-30 burndown work) | Pipeline + Verify                      |
| 7   | Offline-mode network-tap proves no code POSTed externally                              | **NOT-IN-THIS-BATCH** — Cipher + Verify deliverable per V2.1 burndown; no `tests/security/v2.1-offline-network-tap.spec.ts` on disk                                                                                                                                                                                                             | Cipher + Verify                        |
| 8   | Self-dogfood gate V2.1: `audits/v2_1.json` = PASS or PASS WITH FIXES                   | **THIS DOCUMENT** — PASS WITH FIXES, score 80. `audits/v2_1.{json,md}` to be written post-verdict.                                                                                                                                                                                                                                              | Jury ✓                                 |

**Exit-gate roll-up:** 4 GREEN, 1 GREEN-WITH-FILENAME-ALIAS-PENDING, 1 GREEN-WITH-NOTICE-PENDING, 1 SCHEMA-DIVERGENT (V2 carry), 2 NOT-IN-THIS-BATCH (clean-VM CI runner + offline-network-tap test — Pipeline + Cipher + Verify week-29-32 deliverables). Net: **PASS WITH FIXES** for V2.1 Batch 1. The product surface (scaffold generator + customer-facing routes + audit-gate-on-scaffold) is shipped and integration-tested; the operational-validation surface (clean-VM CI + offline-network-tap) is week-29+ deliverable per the burndown.

---

## 3. Cross-cutting findings (cumulative carries status)

### 3.1 V2 carries — status walk

| V2 finding                                                                      | V2.1 status                                                                                                                                                                                                                                                                                                                                                                                | Closer                     |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------- |
| **VF1-V2** (Major — Art. 50 paragraph drift in `bundle-assembler.ts`)           | **STILL OPEN at V2.1 close** — Forge fixed `pr-opener.ts` (V1.5 VF1) but did NOT fix `bundle-assembler.ts` (V2 VF1). Drift now one emitter wide (V2 bundle surface). Re-classified as **chronic** per the panel's V2 prediction.                                                                                                                                                           | Forge — P0 V2.2            |
| **VF2-V2** (Major — Build-mode token cap not implemented)                       | **STILL OPEN at V2.1 close** — no `build_mode` flag in gateway; no `build_token_budget_micros` column; no `check_build_token_budget` function. V2.1 AMPLIFIES the gap (scaffold is an additional layer-lead loop). System Card v1.6 §7 R1 + Compliance §2.10 + §2.15 LIVE status overstated.                                                                                               | Forge + Atlas — P0 V2.2    |
| **VA1-V2** (Major — dual divergent roadmap-bundle schemas)                      | **STILL OPEN at V2.1 close** — both `apps/runner/schemas/roadmap-bundle.v1.schema.json` (Forge, flat documents shape) and `architecture/schemas/roadmap-bundle.v1.schema.json` (Atlas, structured shape) on disk. The bundle assembler still emits the Forge shape; the legal/migration cross-refs still cite the Atlas shape. Schema-as-contract violation per Phase 5 lesson UNRESOLVED. | Forge + Atlas — P0 V2.2    |
| **VF3** (Major chronic — six-milestone — `pinned-versions.json` missing)        | **CLOSED at V2.1** — file shipped at `apps/runner/src/llm/pinned-versions.json` with v1 schema + Anthropic primary/fast + OpenRouter fallback + rotation policy + `pinned-versions.test.ts`. Module-init load assertion in `llm-gateway-client.ts`. **Six-milestone chronic CLOSED.** AI System Card v1.6 §3.1 should flip from HUMAN-pending to LIVE on next re-cut.                      | Forge ✓                    |
| **VF4-V2** (Minor — filename + event-vocabulary drift)                          | **STILL OPEN at V2.1 close** — no thin filename-alias wrappers at `tests/integration/build-audit-gate.spec.ts` or `tests/integration/build-dashboard-reconstruction.spec.ts`. The migration's `state_transition` / `layer_complete` event names still drift from the runner's `phase_started` / `phase_finished` / `layer_started` / `layer_finished` vocabulary.                          | Verify — V2.2              |
| **VF5-V2** (Minor — event vocabulary drift in 0009 header)                      | **STILL OPEN at V2.1 close** — same root as VF4-V2.                                                                                                                                                                                                                                                                                                                                        | Atlas or Forge — V2.2      |
| **VC1-V2** (Minor — pentest report file not on disk)                            | **STILL OPEN at V2.1 close** — `compliance/pentest-firecracker-2026-qN.pdf` not in tree; Compliance §2.11 honesty discipline still flags as `LIVE — POST-PENTEST`. Shield deliverable; week-26-28 burndown.                                                                                                                                                                                | Shield                     |
| **VC2-V2** (Minor — second-provider lane claimed LIVE without on-disk evidence) | **CLOSED at V2.1** — `pinned-versions.json` carries `fallback_provider: "openrouter"` + the OpenRouter model pins. R9 partial closure landed. AI System Card §7 row should update to PARTIAL-LIVE (gateway-side OpenRouter SDK integration is still V2.2 work — the pin contract is in place but the actual provider-switching code is not exercised in production yet).                   | Cortex (full close) — V2.2 |

**Cumulative score:** of the 7 V2 carries, **2 closed** (VF3 + VC2 partial) and **5 still open** at V2.1 close. Three of those five (VF1-V2, VF2-V2, VA1-V2) are Major; the dispatch focused on V1.5 + M5 carries rather than V2 carries, so this is the expected outcome — but the V2 audit's prediction that VF1's drift pattern would become **chronic if it survived V2.1** has now landed. Mark VF1-V2 + VF2-V2 + VA1-V2 as **chronic at V2.2 entry**.

### 3.2 M5 launch-gate carries — status walk

| M5 carry                                                  | V2.1 status                                                                                                                                                | Closer                      |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| CSP + HSTS in `apps/web/next.config.ts`                   | **CLOSED at V2.1**                                                                                                                                         | Forge ✓                     |
| `status-page.spec.ts` filename alias                      | **CLOSED at V2.1** (128 lines, new on-disk spec)                                                                                                           | Forge ✓                     |
| `retention-purge.spec.ts` filename alias                  | **CLOSED at V2.1** (144 lines)                                                                                                                             | Forge ✓                     |
| `gdpr-right-to-delete.spec.ts` filename alias             | **CLOSED at V2.1** (198 lines)                                                                                                                             | Forge ✓                     |
| AT-recording `.webm` capture (NVDA + VoiceOver fail-flow) | **PARTIAL at V2.1** — zero-byte placeholders at the named paths; Forge signoff.md honestly notes "Halo + Vega capture the actual recordings"; **NOT done** | Halo + Vega — V2.2 / M5+30d |

**Cumulative score:** 4 of 5 M5 carries CLOSED at V2.1. The remaining one (actual AT-recording capture) is a Halo+Vega manual-capture task; the placeholder files are sufficient to unblock CI grep-gates that assert file presence but insufficient to satisfy WCAG audit evidence requirements. M5 production-launch T-0 gate is not yet closed.

### 3.3 V1.5 carries — status walk

| V1.5 carry                                                               | V2.1 status                                                                                                                       | Closer   |
| ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | -------- |
| **V1.5 VF1** (Art. 50 paragraph drift in `pr-opener.ts`)                 | **CLOSED at V2.1** — verbatim `legal/pr-body-template.md` §2 paragraph in `ART50_DISCLOSURE` constant + spec exact-string-asserts | Forge ✓  |
| Other V1.5 carries (audit_action enum extensions, ARCH-D7 pattern, etc.) | **CARRY GREEN** — V1.5 controls suite re-verified in V2.1 compliance audit §2.18                                                  | Comply ✓ |

### 3.4 NEW V2.1-specific carries

- **VF1-V2.1** (Major — V2 carry NOT closed; VF1-V2 chronic — `bundle-assembler.ts` Art. 50 paragraph drift) — V2.2 dispatch.
- **VF2-V2.1** (Major — V2 carry NOT closed; VF2-V2 chronic — Build/Scaffold token cap not implemented) — V2.2 dispatch.
- **VF3-V2.1** (Minor — scaffolded `next.config.ts` CSP subset lacks docblock explaining the asymmetry) — V2.2 dispatch.
- **VC1-V2.1** (Minor — Forge scaffolders do not emit the `NOTICE` file the legal extension contractually promises) — Forge close ~15min.
- **VC2-V2.1** (Minor — Compliance audit cites `tests/integration/scaffold-audit-gate.spec.ts`; on-disk file is `scaffold-audit-gate-fail.spec.ts`) — Verify close ~5min filename alias.

### 3.5 Art. 27 EU representative + DMCA Designated Agent — HUMAN-pending (Jo)

Unchanged from V2 + V1.5 + M4. Engagement letter pre-drafted; Prighter recommended at €690/yr; Jo signs. V2.1 Compliance audit §2.20 honestly flags as HUMAN-pending. Closure target re-set to pre-V2.1-close-Plus-30d. Not V2.1-ship-blocking; **is** Art. 50 binding (2026-08-02) blocker — **64 days from V2.1 close, on Jo's calendar.**

---

## 4. Self-dogfood gate V2.1 (6-reviewer lens on Scaffold mode new attack surface)

V2.1 introduces the most recursive attack surface in the series: Studio Zero's scaffold generator emits code that customers run in production, and Studio Zero's own audit gate evaluates that code before delivery. The 6 reviewers each have a lens on this surface:

| Reviewer    | V2.1 finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Verdict       |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| **Halo**    | Scaffold a11y depends on the target stack. `nextjs-saas` ships `app/layout.tsx` + `app/page.tsx` with semantic HTML and the Art. 50 disclosure paragraph in the landing — but does not include an `aria-live` region, a skip-link, or a focus-visible style for the customer's own dashboard. The scaffolded customer's a11y posture is the customer's responsibility per §2A.5, but Studio Zero's scaffold-generation gate could surface this as a Halo finding in the audit-rubric output. The `decideVerdict` table currently treats only Critical/Major/Minor counts — there is no Halo-specific scaffold finding hook beyond the generic mock. **PASS WITH FIX (Minor — scaffold a11y findings not surfaced in the audit rubric output).**                                                 | PASS WITH FIX |
| **Optic**   | Scaffold's UI matches Pixel's design system tokens. `nextjs-saas` ships Tailwind atomic classes via the `tsconfig.json` + a `next.config.ts` posture that mirrors Studio Zero's own. The scaffold does NOT ship `brand-tokens.json` by default (the bundle delivers it separately) — a customer who wants Direction A tokens has to copy from the bundle's `brand-tokens.json` to the scaffolded `tailwind.config.ts`. **PASS WITH FIX (Minor — Optic + Pixel — scaffold-to-bundle token wiring documented but not auto-applied).**                                                                                                                                                                                                                                                             | PASS WITH FIX |
| **Proof**   | Scaffold copy: the README's project description and the landing page hero are LLM-generated from `brief.idea_summary` + `brief.target_audience.persona`/`primary_need`. The fallback (template default — `appPageTsx(brief)`) uses Herald-like voice ("For <persona> who need <primary_need>") but the scaffold is for the CUSTOMER's product, not Studio Zero's product. So Herald's voice would be inappropriate to apply directly — the LLM should generate the customer's own voice, derived from the brief. The current pipeline has the LLM emit text per the brief but does NOT seed a customer-voice rubric (no `brief.voice_adjectives`-conditioned prompt). **PASS WITH FIX (Minor — Proof + Herald — customer-voice-conditioned prompt not yet seeded in scaffold code-generator).** | PASS WITH FIX |
| **Compass** | Target persona for V2.1: the customer is now the operator of the scaffolded code (deploy + maintain + extend). The `nextjs-saas` README has install instructions appropriate for a technical solo founder ("`pnpm install`, copy `.env.example`, `pnpm dev`"). The customer-review obligation in §2A.5 is plain-English and named. **PASS.**                                                                                                                                                                                                                                                                                                                                                                                                                                                    | PASS          |
| **Trace**   | Scaffold pipeline state machine: brief → recommendStack → generateBriefSensitiveFiles → scaffolder → audit-gate → zip/refund. Forward path is dead-end-free; abort signal threaded through `RunScaffoldPipelineInput.signal`. FAIL branch emits `failed` event with `refund: true` and returns `deliverable: false` + empty zip_bytes. The customer-facing routes (`/scaffold/page.tsx` progress + `/scaffold/download` CTA) close the post-delivery flow. **PASS.**                                                                                                                                                                                                                                                                                                                            | PASS          |
| **Canon**   | Token discipline on the scaffold's output: each scaffolder uses tokens-by-default (Tailwind atomic classes, semantic HTML, no hex literals in TSX). The Studio Zero brand tokens do NOT flow into the scaffolded `tailwind.config.ts` automatically (see Optic finding). **PASS WITH NOTE (token-flow gap documented under Optic; Canon proper passes).**                                                                                                                                                                                                                                                                                                                                                                                                                                       | PASS          |

**Net self-dogfood V2.1 findings:** **0 Critical / 2 Major carried from V2 (VF1-V2.1 + VF2-V2.1 — chronic) / 5 Minor (VF3-V2.1 + VC1-V2.1 + VC2-V2.1 + Halo scaffold-a11y-hook + Optic+Pixel token-flow + Proof+Herald customer-voice-prompt).**

**Self-dogfood verdict V2.1: PASS WITH FIXES** — consistent with M0–V2 posture. The scaffold surface introduces no Critical findings on the recursive audit-gate path; the open Major findings are V2 carries that survived the V2.1 dispatch (which was scoped to V1.5 + M5 carries). The 5 reviewer-lens Minors are scaffold-quality polish backlog for V2.2.

---

## 5. V2.1 score reasoning per `score_engine.v1.json`

- Starting 100.
- **−6** (VF1-V2.1 / VF1-V2 chronic — `bundle-assembler.ts` Art. 50 paragraph drift NOT closed despite dispatch focus; load-bearing legal disclosure; now spans V2 emitter alone after V1.5 sibling closed; pattern is **two-milestones chronic** for this exact bundle emitter).
- **−5** (VF2-V2.1 / VF2-V2 chronic — Build/Scaffold token cap STILL NOT implemented; only finding where compliance + System Card claim a control LIVE that has zero on-disk technical implementation; V2.1 AMPLIFIES the gap).
- **−3** (VA1-V2 schema divergence carry — dual divergent roadmap-bundle schemas STILL on disk; schema-as-contract violation; two milestones now).
- **−2** (VC1-V2.1 NOTICE file not emitted + VC2-V2.1 filename alias + VF3-V2.1 CSP subset docblock — three V2.1-specific Minors).
- **−1** (VF4-V2 + VF5-V2 + VC1-V2 + 5-reviewer-lens Minors — V2.1 polish backlog).
- **+3** (V1.5 VF1 closed; VF3 six-milestone chronic CLOSED; 4 of 5 M5 carries closed — strongest single-batch carry-closure rate in the series).
- **+1** (defense-in-depth tightening on `scaffold_jobs_delivered_verdict_required` CHECK adds `generated_artifact_id IS NOT NULL` beyond V2's pattern).
- **+1** (the recursive audit-gate architecture — Studio Zero audits its OWN scaffold output with the gate it sells — is the cleanest expression of the meta-product thesis in the series).
- **+1** (Atlas+Comply §2A insertion choice preserves all cross-references — discipline learned from M3/M5 filename-alias carry pattern).
- **= 80/100** (matches V2's 80; ties for series-second; -2 from M5's 82).

Score reasoning: V2.1 is **+0 from V2**: the strong-carry-closure pattern (4 of 7 named carries + V1.5 + M5) is offset by V2's three Majors (VF1 / VF2 / VA1) surviving the dispatch. The recursive audit-gate architecture earns a polish-positive but is not enough to overcome the V2 chronics. The −2 versus M5's 82 reflects (a) the V2 Major-chronics now established, (b) the V2.1-specific Minors on the scaffold surface, and (c) the pattern that the dispatch task did not name VF1-V2 + VF2-V2 + VA1-V2 explicitly — a Jury-dispatch coordination gap.

---

## 6. V2.1 exit verdict + V2.2 / Phase 10 readiness

**PASS WITH FIXES — score 80.**

V2.1 Batch 1 ships scaffold-mode to the Managed Pro V2.1 cohort behind the following closures.

**Recommended before V2.1 customer-visible feature flag flip (the four V2.1-specific findings + the carry-over V2 Majors):**

1. **VC1-V2.1 (Minor)** — Forge adds `NOTICE` file emission to all three scaffolders carrying the locked-verbatim Art. 50 paragraph. ~15 min.
2. **VC2-V2.1 (Minor)** — Verify ships filename alias at `tests/integration/scaffold-audit-gate.spec.ts`. ~5 min.
3. **VF3-V2.1 (Minor)** — Forge adds docblock to scaffolded `next.config.ts` template explaining CSP subset choice. ~10 min.
4. **VF1-V2 chronic (Major)** — Forge factors `apps/runner/src/legal/art50-disclosures.ts` constants module; `bundle-assembler.ts` prepends to every emitted markdown artifact; spec exact-string-asserts. ~30 min. **Should NOT wait for V2.2 — the V2 bundle surface ships the wrong Art. 50 paragraph today.**

**Recommended for V2.2 dispatch (the surviving V2 Majors + V2.1 polish backlog):**

5. **VF2-V2 chronic (Major)** — Forge + Atlas implement Build/Scaffold stricter token cap end-to-end (gateway flag + tenants column + SECURITY-DEFINER function + dispatcher propagation). ~2 hours.
6. **VA1-V2 chronic (Major)** — Forge + Atlas reconcile to single canonical `roadmap-bundle.v1.schema.json`. ~2 hours.
7. **VF4-V2 + VF5-V2 (Minor)** — Verify ships filename aliases + Atlas/Forge resolves event-vocabulary drift. ~30 min.
8. **VC1-V2 (Minor)** — Shield delivers Firecracker pentest report. Week-scale.
9. **VC2-V2 full close** — Cortex wires OpenRouter SDK on gateway-side. Week-scale.
10. **Halo scaffold-a11y findings hook in audit rubric** — Halo + Forge. ~1 hour.
11. **Optic + Pixel scaffold-to-bundle token wiring docs** — Optic + Pixel. ~30 min.
12. **Proof + Herald customer-voice-conditioned prompt seed for code-generator** — Proof + Herald + Forge. ~1 hour.
13. **AT-recording capture** — Halo + Vega. Manual capture; M5 production-launch T-0 gate item.
14. **Art. 27 EU representative + DMCA Designated Agent — Jo signatures.** Pre-Art-50-binding-2026-08-02. **64 days from V2.1 close.**

**Series-shape snapshot:** **M0 75 · M1 75 · M2 78 · M3 77 · M4 80 · M5 82 · V1.5 79 · V2 80 · V2.1 80.** V2.1 ties V2. The cumulative chronic count is the structural finding the panel calls out for Phase 10 entry: VF1-V2 + VF2-V2 + VA1-V2 are now two-milestones each; if they survive V2.2 they trigger the BUILD_FLOW.md §"never soften findings to meet a deadline" lesson.

---

## 7. V2.2 readiness signal

V2.2 batch should open with the following ticket priorities, derived from this audit:

| Priority | Item                                                                                                              | Owner                  | Estimate       |
| -------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------- | -------------- |
| P0       | VF1-V2 chronic close — `art50-disclosures.ts` constants module + `bundle-assembler.ts` prepend to all 9 artifacts | Forge + Comply         | 30 min         |
| P0       | VF2-V2 chronic close — Build/Scaffold token cap end-to-end                                                        | Forge + Atlas          | 2h             |
| P0       | VA1-V2 chronic close — schema divergence reconciliation                                                           | Forge + Atlas          | 2h             |
| P0       | VC1-V2.1 — `NOTICE` file emission                                                                                 | Forge                  | 15 min         |
| P0       | VC2-V2.1 — `scaffold-audit-gate.spec.ts` filename alias                                                           | Verify                 | 5 min          |
| P0       | VF3-V2.1 — scaffolded CSP docblock                                                                                | Forge                  | 10 min         |
| P1       | VF4-V2 + VF5-V2 filename aliases + event-vocabulary drift                                                         | Verify + Atlas         | 30 min         |
| P1       | Halo scaffold-a11y findings hook in audit rubric                                                                  | Halo + Forge           | 1h             |
| P1       | Optic + Pixel scaffold-to-bundle token wiring docs                                                                | Optic + Pixel          | 30 min         |
| P1       | Proof + Herald customer-voice-conditioned prompt for code-generator                                               | Proof + Herald + Forge | 1h             |
| P1       | Shield Firecracker pentest report (V2 carry)                                                                      | Shield                 | week-scale     |
| P1       | Cortex OpenRouter SDK gateway-side integration (R9 full close)                                                    | Cortex                 | week-scale     |
| P2       | M5 AT-recording capture (NVDA + VoiceOver)                                                                        | Halo + Vega            | manual         |
| P2       | Pipeline clean-VM CI runner (Linux + Windows + macOS)                                                             | Pipeline + Verify      | week-scale     |
| P2       | Cipher offline-mode network-tap test                                                                              | Cipher + Verify        | week-scale     |
| HUMAN    | Art. 27 EU representative + DMCA Designated Agent signatures                                                      | Jo                     | pre-2026-08-02 |

Jury locks V2.1 verdict: **PASS WITH FIXES — score 80.** V2.1 Batch 1 ships to the Managed Pro V2.1 cohort. The four ≤30-min closures should land as a V2.1 hotfix before customer-visible feature flag flip. The three V2 Majors (VF1 / VF2 / VA1) are now chronic and must close at V2.2. BigBrain signs.

---

_Audit drafted by Jury on 2026-05-12, Phase 9 V2.1 Batch 1, against `sprint/milestone-V2-1.md` exit gate + `PRD.md` §16 V2.1 + §7.3 + `BUILD_FLOW.md` Phase 9 + EU AI Act Art. 50 + the V2 audit's enumerated carries. Self-dogfood gate V2.1 applied._

_Jury sign: 2026-05-12._
