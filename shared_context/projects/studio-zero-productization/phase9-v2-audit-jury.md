# Phase 9 — V2 Audit (Jury)

**Auditor:** Jury (orchestrator + 6-reviewer lens — Halo / Optic / Proof / Compass / Trace / Canon)
**Date:** 2026-05-12
**Scope:** V2 Batch 1 — commits `372aafe` (Forge: Build mode end-to-end — 5 runner modules + 6 web routes + 4 build pages + Edge Functions `build-brief` + `build-audit-gate` + 3 integration specs) and `a1de2a0` (Atlas+Comply: `0009_build_mode_v2.sql` 926 lines + `brief.v1.schema.json` + `roadmap-bundle.v1.schema.json` (architecture/) + Build mode ToS extension + AI System Card v1.5 + V2 compliance scorecard). Audited against `sprint/milestone-V2.md` exit gate + `PRD.md` §4 V2 + §7.3 Build Workflow + `BUILD_FLOW.md` Phase 9 + EU AI Act Art. 50 binding 2026-08-02.
**Self-dogfood gate V2:** APPLIED (6-reviewer lens on V2 new attack surface — multi-layer build agents authoring customer's GitHub repository under the Studio Zero GitHub App's write scope).
**Verdict at V2 close:** **PASS WITH FIXES — score 80.** Above the 70 PASS threshold; one point above V1.5's 79. The V2 producers (Forge for backend; Atlas+Comply for data+legal) shipped the cleanest cross-producer batch of the series with strong defense-in-depth (DB-side CHECK + RLS Edge-Function-only gate + integration test trio); however three new V2-specific findings and three carried chronic items hold the score below M5's 82.

Series shape: **M0 75 · M1 75 · M2 78 · M3 77 · M4 80 · M5 82 · V1.5 79 · V2 80.** V2 is +1 from V1.5 (Auto-PR risk tempered by the Build mode two-gate human oversight structure and the DB-enforced `builds_delivered_verdict_required` CHECK), −2 from M5 (cumulative chronic carries — `pinned-versions.json`, Art. 27 rep, CSP+HSTS, test filename aliases, AT recordings — still not closed across four milestones now; V2 introduces the schema-divergence finding as well).

---

## 1. Per-producer verdict

### 1.1 Forge — commit `372aafe` — **PASS WITH FIXES (Major + Major + Minor)**

**4,441 net lines across 21 files.** Five runner-side V2 modules (`apps/runner/src/build/v2/`): `types.ts` (117 lines — `Brief` / `LayerOutput` / `BuildEvent` / `RoadmapBundle` typed contracts), `layer-dispatcher.ts` (288 lines — 2-wave dispatch over 8 layers with `phase_started`/`phase_finished` and `layer_started`/`layer_finished` events), `bundle-assembler.ts` (184 lines — pure-string synthesis of 10 markdown/JSON documents), `repo-seeder.ts` (238 lines — `GhSeederClient` interface matching V1.5 `pr-opener` shape, milestones-from-roadmap derivation, collision retry, label backfill). Six web routes (`/api/builds`, `/api/builds/[buildId]`, `confirm-brief`, `dispatch-layers`, `cancel`). Four pages (`/app/builds`, `/app/builds/new` 6-step intake wizard, `/app/builds/[buildId]` live dashboard with `aria-live="polite"`, `/app/builds/[buildId]/output` tabbed deliverables viewer). Two Edge Functions: `supabase/functions/build-brief/index.ts` (340 lines — BigBrain brief generator) + `supabase/functions/build-audit-gate/index.ts` (260 lines — same service-role pattern as V1.5 `jury-reaudit-gate`, 20-min poll, mock-mode escape, DB-enforced transition to `audit_gate_passed`/`audit_gate_failed`). Three integration specs: `build-mode-happy-path.spec.ts` (3 tests — full dispatch chain + milestone extraction + ajv-schema validation), `build-audit-gate-fail.spec.ts` (2 tests — FAIL ⇒ `seedRepo` never called + FAIL bundle still ajv-valid), `build-cancel-mid-flight.spec.ts`.

**What Forge nailed:**

- **DB-enforced two-gate human oversight (PRD §7.3 + ARCH-D7 pattern reuse).** The `build-audit-gate` Edge Function is the ONLY transition path to `builds.state='audit_gate_passed'` or `audit_gate_failed` — same service-role-bearer-gated pattern Forge proved at V1.5 (`jury-reaudit-gate`). The DB rejects unauthorized callers even if app code is buggy. Negative test `build-audit-gate-fail.spec.ts` asserts the gate decision tree: with `audit_gate.verdict='FAIL'`, the `seedRepo` GitHub-client spy records ZERO calls; with `verdict='PASS WITH FIXES'`, seeding proceeds; ajv-validation passes on both shapes including the `rejection_reason` field. This is the load-bearing V2 exit-gate assertion — green.
- **Live-dashboard reconstructibility from event stream alone (V2 exit-gate MI5 analog).** `layer-dispatcher.ts` emits exactly one `phase_started` at the top of the dispatch, one `layer_started` + one `layer_finished` per layer, and one `phase_finished` at the bottom. `build-mode-happy-path.spec.ts` asserts `new Set(startedLayers) === new Set(expectedLayers)` for all 8 layers — the dashboard event stream is structurally complete. The web client at `/app/builds/[buildId]/page.tsx` subscribes to `build:<buildId>` Realtime channel with polling fallback.
- **Wave-1/Wave-2 dependency graph + abort signal propagation.** `WAVE_1=["strategy"]`, `WAVE_2=["design","data","security","growth","frontend","backend","ops"]` — matches PRD §7.3 + BUILD_FLOW.md phase-2-→-8 ordering compressed for the V2 amplified-LLM Build run. `signal: AbortSignal` threads through `dispatchLayers` → `runLayer` so the cancel-mid-flight route propagates correctly.
- **6-step intake form a11y discipline.** `/app/builds/new` uses `<fieldset>` + `<legend>` per step, `aria-live="polite"` on the step indicator, error-summary focus trap; the wizard validates per-step (project name ≥3 char + idea ≥20 char on step 1; persona + need + pain on step 2; vibe adjectives on step 3; output-preference enum on step 5). Optic Hick's-Law check: each step exposes ≤7 affordances; the wizard collapses what would otherwise be a 27-cell combinatorial intake into a serialized 6-step flow with smart defaults (BUILD_FLOW.md Phase 3 lesson).
- **GhSeederClient interface reused from V1.5.** `repo-seeder.ts` uses the same mocking pattern as `auto-pr-flow.spec.ts` — `createRepo` / `putFile` / `createMilestone` / `createIssue` are minimal contract methods the test fakes implement directly. No transitive dependency on Octokit in tests; the runner injects the real client.
- **Cancel-mid-flight refund path.** `/api/builds/[buildId]/cancel/route.ts` (137 lines) honors the extension §6.1 quota-consumption rule: builds in `created`/`queued`/`brief_generating`/`brief_pending_confirmation` cancel without burning Managed Pro inclusion; builds in `layers_dispatching` and later capture the audit_action ledger row.

**Forge Major Jury-gated findings:**

- **VF1-V2 (Major) — Art. 50 paragraph drift carries to Build mode.** Compliance §2.9 + System Card v1.5 §8 declare the per-bundle Art. 50 disclosure paragraph **LOCKED VERBATIM** (same Comply gate as the Auto-PR PR-body template §2): _"This document was generated by Studio Zero v1.5 from a customer-confirmed brief. AI-generated content (Art. 50 EU AI Act 2024/1689). Reviewed by the Studio Zero audit gate before delivery; customer reviews before publication."_ The Forge `bundle-assembler.ts:140-159` emits **a different paragraph**, scoped to the README only (not every artifact as System Card §8 + Compliance §2.9 explicitly require), reading: _"This bundle was produced by Studio Zero. See https://studio-zero.app/ai-system-card for the System Card v1.0."_ — wrong version reference (v1.0 vs v1.5), wrong domain (`studio-zero.app` vs `studiozero.dev`), no Art. 50 binding citation, no GDPR-Art-22-cleared human-oversight language, NOT on the roadmap/architecture/PRD/brand-tokens/voice/decisions/risks/COGS/channels documents. Mirror of V1.5 VF1 in a new emitter. **Closure (~30 min):** Forge factors the Comply-locked paragraph into a single `ART50_BUNDLE_DISCLOSURE` constant (parameterized for system-card version stamp), prepends it to every emitted markdown artifact in `bundle-assembler.ts`, and updates `build-mode-happy-path.spec.ts` to exact-string-assert the disclosure on at least three artifacts (README, PRD, voice).
- **VF2-V2 (Major) — Per-tenant Build-mode stricter token cap is documented in a comment but NOT implemented.** `layer-dispatcher.ts:30-33` carries an explicit specification comment: _"per-tenant token cap is stricter for Build mode than for Audit mode (8x the per-audit budget across the 8 layers). Enforcement lives in the LLM gateway — the dispatcher passes a `build_mode: true` flag the gateway uses to pick the build-mode budget."_ No such flag is passed to the gateway; `supabase/functions/llm-gateway/index.ts:160-235` runs the unified R1 `check_token_budget()` path identically for audit + build with no Build-mode arm. AI System Card v1.5 §7 NEW V2 row "LLM cost amplification" claims the mitigation is LIVE; Compliance §2.10 marks it LIVE. The mitigation as documented does not exist in code — the cap is the same 110% R1 cap that applies to audit mode. **Closure (~2 hours):** (a) llm-gateway accepts a `build_mode: boolean` flag in the request body and picks an alternate budget multiplier when true; (b) `layer-dispatcher.ts` propagates the flag through `runLayer` → gateway call; (c) Atlas adds a `tenants.build_token_budget_micros` column OR a SECURITY-DEFINER `check_build_token_budget(p_tenant_id)` function that returns false at the stricter ceiling. Until closed, Penny's V2 unit-economics claim and Compliance §2.10 LIVE status are both overstated.
- **VF3 (Major chronic — M3→M4→M5→V1.5→V2 carry) — `runner/llm/pinned-versions.json` STILL MISSING.** Five milestones of carry now. AI System Card v1.5 §3.1 honestly flags as HUMAN-pending again ("status unchanged from v1.0"). The Card's v1.6 close target (V2-close + M3-close) is now operational; quarterly re-verification 2026-08-12 falls during V2.1 work and Art. 50 binds 2026-08-02. **Jury upgrades urgency:** close at V2 + 5 days OR explicit BigBrain-signed deferral in `decisions.md` documenting why the drift-dashboard fallback (gateway-config snapshot) is acceptable through 2026-08-12. The honesty in the System Card prevents this from becoming non-conformance, but the chronic pattern is the failure mode `BUILD_FLOW.md` Phase 9 ("never soften findings to meet a deadline") calls out.

**Forge Minor findings:**

- **VF4-V2 (Minor) — Exit-gate test-filename mismatch carry (same M5/V1.5 pattern).** `sprint/milestone-V2.md` L96 and migration `0009_build_mode_v2.sql` L13–L22 cite tests by paths that do not exist on disk: `tests/integration/build-audit-gate.spec.ts` (shipped as `build-audit-gate-fail.spec.ts`); `tests/integration/build-dashboard-reconstruction.spec.ts` (no on-disk equivalent — coverage IS in `build-mode-happy-path.spec.ts` which asserts every layer's started+finished event emit, but the named file is absent). Five cross-references in compliance + migration + milestone + test-strategy + ia/sitemap, all to the wrong path. CI grep-gate on the exit-gate path will fail. **Closure (~15 min):** Verify ships thin filename-alias wrappers at the exit-gate-named paths re-exporting from the on-disk specs.
- **VF5-V2 (Minor) — Migration header references events `state_transition` + `layer_complete` but runner emits `phase_started` / `phase_finished` / `layer_started` / `layer_finished`.** `0009_build_mode_v2.sql:21–22` reads "reconstructible solely from emitted state_transition + layer_complete events in build_events" but the `BuildEvent.type` union in `apps/runner/src/build/v2/types.ts:70–77` does not contain those values. The web/Realtime path emits the runner's vocabulary; the migration's schema comment names a different vocabulary. Not a functional gap — events still hit the table — but a documentation/schema-comment drift. **Closure (~15 min):** Atlas updates the migration header comment to match the runner's event names OR Forge adds `state_transition`/`layer_complete` aliases.

### 1.2 Atlas + Comply — commit `a1de2a0` — **PASS WITH FIXES (Major + Minor)**

**2,268 net lines across 8 files.** Migration `0009_build_mode_v2.sql` (926 lines — `build_state` enum 15 values; `build_output_preference` enum 3 values; `builds` ULID-primary-key table with brief jsonb + layer_outputs jsonb + delivered_verdict + audit ledger plumbing; `build_artifacts` immutable rows; `build_events` append-only Realtime feed; 5 `audit_action` enum extensions: `build_brief_confirmed` / `build_layers_dispatched` / `build_audit_gate_passed` / `build_audit_gate_failed` / `build_refunded`; `builds_delivered_verdict_required` CHECK; `builds_audit_gate_update` RLS Edge-Function-only predicate). Two schemas: `architecture/schemas/brief.v1.schema.json` (136 lines) + `architecture/schemas/roadmap-bundle.v1.schema.json` (454 lines — strict-mode bundle contract). Comply: `legal/build-mode-extension.md` (198 lines — load-bearing customer IP ownership clause + similarity disclosure + audit-gate-is-opinion + Managed Pro pricing + retention + sub-processors + plain-English disclaimers); `legal/ai-system-card-v1.5.md` (236 lines — supersedes v1.0; three NEW V2 risk rows; four NEW V2 performance metrics; Build mode two-gate human oversight); `compliance/v2-compliance-audit.md` (295 lines — 16-control V2 scorecard); ToS bump v1.1 → v1.2 incorporating extension by reference §2.5.

**What Atlas+Comply nailed:**

- **DB-side defense-in-depth on audit gate (mirror of V1.5 0008 pattern, applied to Build mode).** Migration §D `builds_delivered_verdict_required` CHECK: `CHECK (state <> 'delivered' OR (delivered_verdict IN ('PASS','PASS WITH FIXES') AND delivered_at IS NOT NULL))` — DB rejects any attempt to transition to delivered with a FAIL verdict. Migration §H.1 `builds_audit_gate_update` RLS: only the Edge-Function JWT with `iss='supabase-edge-functions'` AND `role='build_gate'` may transition `builds.state` to `audit_gate_passed` or `audit_gate_failed`. Member JWTs and runner JWTs are rejected. Two independent layers: CHECK + RLS. Verify exit-gate test (Forge's `build-audit-gate-fail.spec.ts`) proves the gate.
- **Customer IP ownership clause is load-bearing AND first-substantive-section.** `legal/build-mode-extension.md` §2 opens with: _"**You own the output Studio Zero produces for your build.**"_ — the four-paragraph enumeration covers roadmap+docs bundle, seeded GitHub repository (transfers at seeding moment), V2.1 scaffold, and derived works. §2.1 copyright assignment is unambiguous; §2.2 retains the rubric + score engine + agent system + runner ("we sold you a printing press" analogy is concrete); §2.3 license-back is bounded ("does not authorize you to operate a competing audit-as-a-service product"). Plain-English mandate satisfied: a non-lawyer customer reading the first 200 words knows who owns what.
- **Similarity-to-public-references disclosure is honest, not hedged.** §4 names the mechanism (foundation-model trained on public material), names the verification responsibility on the customer ("you are responsible for verifying… before you publish"), names that Studio Zero does NOT run a similarity search before delivery, opens a dispute path with Comply case-by-case refund. AI System Card v1.5 §7 NEW V2 row matches; Performance metrics §9 NEW V2 row "similarity dispute rate <5%" is the SLO for honesty.
- **Audit-gate-is-opinion disclaimer (§5).** Names four things the gate is NOT (legal opinion, security pentest, patent/trademark clearance, substitute for human judgment); §5.1 PASS WITH FIXES delivers with risks/decisions surfaced; §5.2 FAIL refuses delivery + refund per regional matrix + Comply-attributable re-submit at no charge. Mirrors the Auto-PR re-audit refund framework from V1.5.
- **AI System Card v1.5 honesty discipline maintained.** §3.1 still flags `pinned-versions.json` HUMAN-pending without pretending the artifact exists; §1 still flags Art. 27 HUMAN-pending; §12.1 changelog records v1.0 → v1.5 with the v1.6 close target. Performance metrics §9 NEW V2 rows are framed as "measured at V2+30d" — the card does not claim data it does not have. Build mode two-gate structure (brief confirmation + bundle review) is documented as the GDPR Art. 22 clearance.
- **Compliance scorecard §1 16-control matrix is the cleanest of the series.** Twelve LIVE controls each with a hard-blocker column; two RE-VERIFIED carry-overs from V1.5/M4; two HUMAN-PENDING (DMCA + Art. 27); one N/A (biometrics). §3 V2-exit-gate cross-reference table maps every sprint milestone L96–L101 row to a scorecard control or sibling agent owner.

**Atlas+Comply Major Jury-gated findings:**

- **VA1-V2 (Major) — DUAL DIVERGENT roadmap-bundle schemas in tree.** TWO schema files exist with **substantively different shapes**:
  - `apps/runner/schemas/roadmap-bundle.v1.schema.json` (132 lines, Forge-shipped) — pinned `build_id`/`tenant_id`/`brief_ref` as UUIDs; `documents` is a flat 10-string-keys object; `layer_outputs[]` carries `layer`/`agent`/`status`/`summary`; `audit_gate` enum is `PASS`/`PASS WITH FIXES`/`FAIL`.
  - `architecture/schemas/roadmap-bundle.v1.schema.json` (454 lines, Atlas-shipped) — `build_id` is ULID Crockford-26 (`^[0-9A-HJKMNP-TV-Z]{26}$`); top-level shape is `roadmap[]`/`architecture`/`prd`/`brand`/`voice`/`decisions[]`/`risks[]`/`cogs`/`channels`/`readme` (NOT the flat `documents` object); requires `score_engine_version` + `score` + `project_name` at top level; `roadmapMilestone`/`prdDoc`/`brandDoc`/`voiceDoc` etc. as structured `$defs`. The Atlas schema is the one cited by `legal/build-mode-extension.md` (cross-ref) and `0009_build_mode_v2.sql` header (L11–L13: "architecture/schemas/roadmap-bundle.v1.schema.json committed + ajv-validates"). The Forge schema is the one validated by `build-mode-happy-path.spec.ts:194-197` and `build-audit-gate-fail.spec.ts:165-168`. **The bundle the runner produces ajv-validates against the Forge schema but FAILS ajv-validation against the Atlas schema** (different required keys, different shapes, different ID format). This is a schema-as-contract violation per BUILD_FLOW.md Phase 5 lesson learned: _"schemas-as-files, not schemas-as-prose. The first time a reviewer adds a field to the audit output without updating the schema, downstream consumers silently diverge."_ Two producers shipped two divergent schemas for the same artifact name in the same batch. **Closure (~2 hours):** decide which schema is canonical (recommend Atlas's — it is the legal-cross-ref target and the richer shape), retire the other, update `build-mode-happy-path.spec.ts` + `build-audit-gate-fail.spec.ts` to ajv-validate against the canonical path, update `bundle-assembler.ts` to emit the canonical shape (ULID `build_id`, structured `roadmap[]` instead of `roadmap_md` string, etc.). If Forge's flat shape is preferred for V2 delivery, Atlas re-versions to `roadmap-bundle.v2.schema.json` and the ToS extension + migration cross-refs update. Until closed, the V2 exit-gate item "`runner/schemas/roadmap-bundle.v1.schema.json` committed + ajv-validates a sample bundle" is technically green for the file Forge ships but the cited canonical reference in the ToS + migration is the divergent Atlas file. This is the V2-specific schema-as-files violation Phase 5 explicitly warns about.

**Atlas+Comply Minor findings:**

- **VC1-V2 (Minor) — Compliance §2.11 claims Firecracker pentest report LIVE but report file not on disk.** Scorecard §2.11 reads: "_pentest report committed at `compliance/pentest-firecracker-2026-qN.pdf` with ≤1 Major / 0 Critical (V2 exit gate). HUMAN-pending verification: the actual report file commits once Shield's engagement closes._" The control row is marked **LIVE — POST-PENTEST** but the parenthetical admits the file is not yet committed. This is honest documentation in §2.11 but misleading in §1's `LIVE` cell + the §4 audit-conclusion's "twelve LIVE controls" count. **Closure:** restate §1 + §4 as `LIVE-PENDING (Shield report file)` until the PDF lands. Comply tracks weekly per §2.16-style cadence. Not V2-ship-blocking by the milestone's own framing (V2 launches to existing Managed Pro audience), but the scorecard's claim is one shade overstated.
- **VC2-V2 (Minor) — Second LLM provider lane claimed LIVE without on-disk evidence.** Compliance §2.12 marks "Second LLM provider lane (R9 mitigation closure)" as **LIVE** with evidence "Cortex deliverable per `sprint/milestone-V2.md`; OpenRouter or direct second-provider lane in `runner/llm/`; circuit-breaker fallback live per threat-model TB-4 D-row." The `runner/llm/` directory does not exist on disk (same chronic referenced by VF3). No commit in `git log` between M5 and V2 Batch 1 adds an OpenRouter integration; the `supabase/functions/llm-gateway/index.ts` calls only `https://api.anthropic.com/v1/messages`. The R9 mitigation as claimed LIVE in the System Card v1.5 §7 row "Provider concentration on Anthropic" is not actually live. **Closure (~1 day):** either Cortex ships the second-provider lane (closes R9) OR Comply downgrades §2.12 + System Card §7 row to PARTIAL with a closure target (V2+30d or V2.1).

### 1.3 Producer-pair joint verdict

The two commits land together as a coordinated batch with strong cross-referencing: the migration cites the schema + extension + System Card; the extension cites the schema + migration + System Card; the System Card cites all three plus the scorecard; the scorecard cites everything. Cross-ref hygiene is the strongest of the series. The schema divergence (VA1-V2) is the integration gap that this hygiene almost-but-not-quite caught — the cross-refs all point at the Atlas schema but Forge implemented and tests validate against the Forge schema. The fix is in the integration plane, not in either producer's lane individually.

VF1-V2 (Art. 50 paragraph drift) mirrors VF1 in V1.5 — same compliance-vs-emitter integration gap pattern, in a new emitter. Two milestones now the audit identifies this pattern; if it appears again at V2.1 it should be re-classified as **chronic**.

VF2-V2 (token cap claimed but not implemented) is the most serious finding — it is the only one where the compliance scorecard claims a control LIVE that has zero on-disk technical implementation. Penny's unit-economics math for Build mode depends on this cap; without it, R1 mitigation does not actually scale for Build mode amplification.

---

## 2. V2 exit-gate scorecard (per `sprint/milestone-V2.md` L94–L100)

| #   | Exit-gate item                                                                                                     | Status at HEAD                                                                                                                                                                                                                            | Closer                            |
| --- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- |
| 1   | `runner/schemas/roadmap-bundle.v1.schema.json` committed + ajv-validates a sample bundle                           | **GREEN at the Forge path** `apps/runner/schemas/roadmap-bundle.v1.schema.json` validated by `build-mode-happy-path.spec.ts` (3/3) — but **DIVERGENT** from Atlas's `architecture/schemas/roadmap-bundle.v1.schema.json` (VA1-V2)         | Forge + Atlas (reconcile schemas) |
| 2   | Audit-gate-blocks-delivery negative test green — `tests/integration/build-audit-gate.spec.ts`                      | **NOT-ON-DISK at the named path; coverage IN `tests/integration/build-audit-gate-fail.spec.ts` 2/2 green** (VF4-V2 — same M5/V1.5 filename-alias carry pattern)                                                                           | Verify (filename alias)           |
| 3   | Firecracker microVM A/B test green — no verdict regression; runtime within 1.5× rootless baseline                  | **NOT-IN-THIS-BATCH** — Shield + Verify deliverable; no `firecracker-ab.spec.ts` on disk; Compliance §2.11 admits report file pending                                                                                                     | Shield + Verify                   |
| 4   | Second external pentest on Firecracker config ≤1 Major / 0 Critical — `compliance/pentest-firecracker-2026-qN.pdf` | **NOT-ON-DISK** (VC1-V2 — Compliance §2.11 admits)                                                                                                                                                                                        | Shield (vendor delivery)          |
| 5   | Live build dashboard reconstructible solely from emitted phase events                                              | **GREEN** — `build-mode-happy-path.spec.ts:103-122` asserts `new Set(startedLayers).equals(new Set(expectedLayers))` for all 8 layers; the dashboard at `/app/builds/[buildId]/page.tsx` subscribes to `build:<buildId>` Realtime channel | Forge ✓                           |
| 6   | Self-dogfood gate V2: `audits/v2.json` = PASS or PASS WITH FIXES                                                   | **THIS DOCUMENT** — PASS WITH FIXES, score 80. `audits/v2.{json,md}` to be written post-verdict.                                                                                                                                          | Jury ✓                            |

**Exit-gate roll-up:** 2 GREEN, 1 GREEN-WITH-FILENAME-ALIAS-PENDING, 1 SCHEMA-DIVERGENT, 2 NOT-IN-THIS-BATCH (Firecracker A/B + pentest report — Shield deliverables for week 26–28 per burndown). Net: **PASS WITH FIXES** for V2 Batch 1 — V2 ship gate is half-tracked-by-Forge/Atlas/Comply, half-pending-on-Shield. Without Shield's two items (A/B test + pentest report), the V2 milestone is not closed; this batch represents the Build-mode product surface in flight ahead of Firecracker graduation.

---

## 3. Cross-cutting findings (cumulative carries)

### 3.1 `runner/llm/pinned-versions.json` STILL MISSING (Forge M3 chronic — six milestones now)

**Status at V2 close: STILL HUMAN-PENDING.** Carry chain: M3 → M4 → M5 → V1.5 → V2. Six milestones unclosed. AI System Card v1.5 §3.1 still honestly flags HUMAN-pending; v1.6 close target now overlaps with Art. 50 binding (2026-08-02). **Jury upgrades to BLOCKER for V2.1 spec-kickoff:** if not closed at V2.1 start, the BUILD_FLOW.md Phase 9 lesson "never soften findings to meet a deadline" is being violated by procedural drift even though every individual carry is honestly documented. The honesty does not substitute for closure. Forge: close at V2 + 5 days OR BigBrain signs explicit deferral in `decisions.md` documenting why the drift-dashboard fallback (gateway-config snapshot) is acceptable through 2026-08-12. **V2.1 dispatch will close.**

### 3.2 V1.5 VF1 Art. 50 drift in pr-opener — NOT YET closed; V2 introduces a parallel drift in bundle-assembler

V1.5 Jury identified the Art. 50 paragraph drift between Comply-locked template and Forge `ART50_DISCLOSURE` constant in `pr-opener.ts`. No commit between V1.5 Batch 1 and V2 Batch 1 closes this. V2 Batch 1 introduces the same pattern in `bundle-assembler.ts` (VF1-V2). The drift is now **two emitters wide** (Auto-PR PR body + Build bundle artifacts). **V2.1 dispatch will close both simultaneously** by centralizing the Comply-locked paragraphs in a single `legal/art50-disclosures.ts` (or `apps/runner/src/legal/art50-disclosures.ts`) constant module that BOTH emitters import + reference the System Card version stamp from a single source.

### 3.3 M5 carries (CSP + HSTS + 3 test aliases + AT .webm) — NOT YET closed

No commit between M5 close (`7acc9f9`) and V2 Batch 1 touches: `apps/web/next.config.ts` CSP/HSTS headers; `tests/integration/status-page.spec.ts` / `retention-purge.spec.ts` / `gdpr-right-to-delete.spec.ts` filename aliases; `tests/a11y/at-recordings/nvda-fail-flow.webm` / `voiceover-fail-flow.webm`. **All five items remain M5 launch-gate blockers; V2 launches to existing Managed Pro audience so V2 ship is not blocked, but the M5 production-launch T-0 gate is.** V2.1 dispatch will close.

### 3.4 V2-specific Build mode customer-side UI gaps — V2.1 carry

The customer-side experience after audit-gate decision is functional but thin: `/app/builds/[buildId]/output/page.tsx` renders the 10 bundle documents tabbed (README/Roadmap/Architecture/PRD/Voice/Decisions/Risks/COGS/Channels/Brand tokens) with the verdict + score + repo link, but **lacks a download-as-zip CTA**, lacks a **share-as-public-verdict link** (analog to the M2 verdict-share URL Halo specified), and renders the `cogs_md` + `channels_md` as raw markdown text rather than as the structured tooling shape the Atlas schema envisions. The FAIL-state branch shows `rejection_reason` and refund-or-revise CTAs (Trace dead-end check green) but the refund-flow integration with Stripe is server-rendered placeholder. **V2.1 dispatch carry** — richer customer-output UI + zip-download + share-link + Stripe-portal-integrated refund.

### 3.5 Art. 27 EU representative — HUMAN-pending (Jo)

Unchanged from V1.5 + M4. Engagement letter pre-drafted; Prighter recommended at €690/yr; Jo signs to start. V2 Batch 1 carries forward honestly in System Card v1.5 §1 + Compliance §2.16. Not V2-ship-blocking; is Art. 50 binding (2026-08-02) blocker. V2.1 close target.

---

## 4. Self-dogfood gate V2 (6-reviewer lens on Build mode new attack surface)

V2 introduces the most expansive new attack surface in the series: build agents make multi-turn LLM calls across 8 layer-leads to author a customer's entire GitHub repository under the Studio Zero GitHub App's write scope. The threat-model TB-12 §3.5 sandbox-escape and the System Card v1.5 §7 NEW V2 rows acknowledge the magnitude. Applying each reviewer's lens:

| Reviewer    | V2 finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Verdict       |
| ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| **Halo**    | Build dashboard a11y: `/app/builds/new` 6-step intake uses `<fieldset>` + `<legend>` per step, `aria-live="polite"` on the step indicator, error-summary focus trap per SC 3.3.1/3.3.3/3.3.7/3.3.8 (file header comment cites these explicitly). `/app/builds/[buildId]/page.tsx` layer-status grid: chip rendering visually distinguishes `queued`/`running`/`complete`/`failed`/`skipped` but the file header does not document the `aria-live` or `role` attribute on the live-status region — Verify needs to confirm the rendered DOM exposes `role="status"` so the layer-status updates are announced (HC1 pattern from V1.5 D23 banner). FAIL-state output page uses Card + Chip; the rejection reason copy is rendered as static text — needs `aria-describedby` linking the verdict chip to the rejection reason. **PASS WITH FIX (Minor — a11y discipline on live regions).** | PASS WITH FIX |
| **Optic**   | Brief intake Hick's-Law check: 6-step wizard splits a 27-cell combinatorial intake into per-step affordances ≤7; step 5 output-preference is 3 options (Hick's-law budget); no step has >7 form fields. The BUILD_FLOW.md Phase 3 lesson "collapse to 2-step pickers with smart defaults" is honored. Wizard navigation: previous/next + step indicator visible; cancel returns to `/app/builds` without burning quota. **PASS.**                                                                                                                                                                                                                                                                                                                                                                                                                                                        | PASS          |
| **Proof**   | Build-mode brand voice on the marketing surface: `apps/web/app/build/page.tsx` (M5 demand-gate placeholder) still says **"coming in 2027"** — Herald's V2 deliverable "Build-mode brand voice — `ideas → roadmap → audit-passing product` positioning" is NOT yet on the public marketing site. The product is launching; the marketing site is still teasing it. **PASS WITH FIX (Minor — Herald carry).** Closure: replace `/build` placeholder with locked Build-mode brand voice copy citing PRD §7.3 workflow; V2.1 batch.                                                                                                                                                                                                                                                                                                                                                          | PASS WITH FIX |
| **Compass** | Non-technical solo founder using Build mode (System Card v1.5 §2.2 primary persona). Audience-fit check: the brief intake wizard does NOT use jargon ("Tell us what you're building" / "Who is it for" / "What's the vibe" rather than "Submit your product requirements document"). The dashboard layer-status grid uses agent names (strategy/design/data/security/growth/frontend/backend/ops) — these may read opaque to a non-technical founder; the file currently lacks tooltip + glossary affordance. **PASS WITH FIX (Minor — Compass + Herald — layer-name tooltips for non-technical persona).**                                                                                                                                                                                                                                                                              | PASS WITH FIX |
| **Trace**   | Build state machine dead-end-free check: 15 states in `build_state` enum traced through the dispatcher + cancel route + audit-gate Edge Function + repo-seeder. Forward path: `created` → `queued` → `brief_generating` → `brief_pending_confirmation` → `brief_confirmed` → `layers_dispatching` → `layers_running` → `bundle_assembling` → `audit_gate_running` → (`audit_gate_passed` → `delivering` → `delivered`) OR (`audit_gate_failed` → `cancelled`). Cancel route has explicit handler for every pre-`delivered` state. Refund mechanic ledgered. FAIL state surfaces in dashboard + output page with refund-or-revise CTA. No dead-ends found in the state machine. **PASS.**                                                                                                                                                                                                 | PASS          |
| **Canon**   | Token discipline on build dashboard: `/app/builds/[buildId]/page.tsx` and `/output/page.tsx` use Button + Card + Chip components (presumed token-only per V1.5 audit). The bundle-assembler emits markdown documents whose rendered styling is governed by the customer's downstream tooling, not Studio Zero's tokens — out of scope for Canon. The brand-tokens.json artifact in the bundle is the customer's, not Studio Zero's. **PASS.**                                                                                                                                                                                                                                                                                                                                                                                                                                            | PASS          |

**Net self-dogfood V2 findings:** **0 Critical / 3 Major (VF1-V2 Art. 50 paragraph drift; VF2-V2 token cap unimplemented; VA1-V2 dual divergent schemas) / 6 Minor (VF3 pinned-versions.json chronic; VF4-V2 test filename aliases; VF5-V2 event vocabulary drift; VC1-V2 pentest report pending; VC2-V2 second-provider not on disk; Halo + Proof + Compass items).**

**Self-dogfood verdict V2: PASS WITH FIXES** — consistent with the M0–V1.5 posture. Score 80 reflects the new V2 attack surface (build agents authoring customer repos) tempered by the strongest DB-defense-in-depth (CHECK + RLS + integration tests) and the strongest legal-architecture cross-referencing of the series.

---

## 5. V2 score reasoning per `score_engine.v1.json`

- Starting 100.
- **−6** (VF1-V2 Art. 50 paragraph drift on bundle artifacts — Major; load-bearing legal disclosure; same Comply-vs-emitter integration gap pattern as V1.5 VF1 in a new emitter).
- **−5** (VF2-V2 Build-mode stricter token cap documented but NOT implemented — Major; only finding where compliance claim has zero on-disk technical implementation; Penny's V2 unit-economics depends on it).
- **−4** (VA1-V2 dual divergent roadmap-bundle schemas — Major; schema-as-contract violation per Phase 5 lesson; two producers shipped two schemas for the same artifact in the same batch).
- **−2** (VF3 pinned-versions.json six-milestone chronic — Major escalating; honesty in System Card prevents non-conformance but procedural drift is the pattern).
- **−1** (VF4-V2 + VC1-V2 + VC2-V2 — Minor; ≤2h aggregate closure).
- **−1** (V1.5 VF1 still unclosed + M5 5-item carry still unclosed — accumulating chronics).
- **−1** (Halo a11y minor + Proof marketing-site copy + Compass jargon-tooltip — UX polish backlog).
- **= 80/100** (vs V1.5's 79; +1 delta).

Score reasoning: V2 is +1 from V1.5 because the cross-producer integration quality (legal/data/runner all reference each other correctly), the two-gate human oversight structure (brief confirmation + bundle review with DB-side enforcement), and the customer-IP-ownership clause are the strongest substantive work the series has shipped. The −2 versus M5's 82 reflects (a) the new and most-expansive attack surface to date, (b) the schema-divergence finding which is structurally new, and (c) the accumulated chronics (`pinned-versions.json` is now six milestones long).

---

## 6. V2 exit verdict + V2.1 readiness

**PASS WITH FIXES — score 80.**

V2 Batch 1 is launch-ready for the Managed Pro V2 cohort behind the following closures.

**Recommended before V2 customer-visible feature flag flip:**

1. **VF1-V2 (Major)** — Forge ships `art50-disclosures.ts` constant module exporting the Comply-locked v1.5 paragraph (parameterized for system-card version stamp); `bundle-assembler.ts` prepends to every artifact (not just README); spec asserts exact-string match. Also closes V1.5 VF1 chronic by importing the same module into `pr-opener.ts`. ~1 hour.
2. **VF2-V2 (Major)** — Forge + Atlas implement the Build-mode stricter token cap end-to-end: `tenants.build_token_budget_micros` column + `check_build_token_budget(p_tenant_id)` function + `llm-gateway` `build_mode: boolean` flag in request body + `layer-dispatcher.ts` flag propagation. Compliance §2.10 status flips from LIVE to genuinely LIVE. ~2 hours.
3. **VA1-V2 (Major)** — Forge + Atlas reconcile to a single canonical `roadmap-bundle.v1.schema.json` (recommend Atlas's richer shape; retire Forge's flat-`documents` shape OR re-version Forge's to v1; update bundle-assembler.ts to emit canonical shape; update specs). ~2 hours.
4. **VF4-V2 (Minor)** — Verify ships filename aliases at `tests/integration/build-audit-gate.spec.ts` and `tests/integration/build-dashboard-reconstruction.spec.ts`. ~15 min.

**Recommended before V2 + 5 days:**

5. **VF3 (Major chronic — six milestones)** — Forge commits `runner/llm/pinned-versions.json` OR BigBrain signs explicit deferral in `decisions.md`. Closes before quarterly re-verification 2026-08-12.

**V2.1 dispatch carries:**

6. V2 customer-side UI richness (zip download + share link + structured cogs/channels rendering + Stripe-portal refund integration).
7. Herald's marketing-site `/build` page replace placeholder with V2 brand voice.
8. Compass + Herald layer-name tooltips for non-technical persona on dashboard.
9. Halo `role="status"` live-region attribute documentation on layer-status grid.
10. M5 carries (CSP+HSTS, 3 test aliases, AT recordings) — V2.1 batch closes M5 production-launch T-0 gate.
11. Shield's Firecracker A/B test (`tests/integration/firecracker-ab.spec.ts`) + pentest report PDF (`compliance/pentest-firecracker-2026-qN.pdf`) — V2 milestone exit-gate items 3+4 in §2.
12. Cortex's second-provider lane in `runner/llm/` (closes R9; closes VC2-V2 + System Card §7 Anthropic-concentration row).
13. Art. 27 EU representative (Jo) + DMCA Designated Agent (Jo) — pre-Art-50-binding-2026-08-02.

**Series-shape snapshot:** **M0 75 · M1 75 · M2 78 · M3 77 · M4 80 · M5 82 · V1.5 79 · V2 80.** V2 +1 from V1.5; −2 from M5. The cumulative chronic count (`pinned-versions.json` six milestones; V1.5 VF1 + M5 5-item carries still unclosed) is now itself a finding the audit panel should consider a structural drift if it survives V2.1 without closure.

---

## 7. V2.1 readiness signal

V2.1 batch should open with the following ticket priorities, derived from this audit:

| Priority | Item                                                                                   | Owner           | Estimate   |
| -------- | -------------------------------------------------------------------------------------- | --------------- | ---------- |
| P0       | VF1-V2 Art. 50 paragraph drift close (+ V1.5 VF1 simultaneous close via shared module) | Forge + Comply  | 1h         |
| P0       | VF2-V2 Build-mode stricter token cap end-to-end                                        | Forge + Atlas   | 2h         |
| P0       | VA1-V2 schema divergence reconciliation                                                | Forge + Atlas   | 2h         |
| P0       | VF4-V2 + V1.5 VF2 + M5 3 named test aliases (combined batch)                           | Verify          | 1h         |
| P1       | VF3 pinned-versions.json chronic close                                                 | Forge           | 2h         |
| P1       | Firecracker A/B test + pentest report (V2 milestone exit gate items 3+4)               | Shield + Verify | week-scale |
| P1       | Second-provider lane R9 close                                                          | Cortex          | week-scale |
| P2       | Customer-side UI richness                                                              | Vega            | week-scale |
| P2       | Marketing `/build` brand-voice copy                                                    | Herald + Pixel  | 1h         |
| P2       | M5 carries (CSP+HSTS + AT recordings)                                                  | Vega + Halo     | 1d         |
| P3       | Compass + Herald layer-name tooltips                                                   | Vega + Herald   | 30min      |

**V2.1 entry prerequisites per `sprint/milestone-V2-1.md`:** Build-mode V2 attach rate ≥10% of paid customers. Until that signal lands, V2.1 should be backlog scoping, not execution.

Jury locks V2 verdict: **PASS WITH FIXES — score 80.** V2 Batch 1 ships to the Managed Pro V2 cohort. Sprint disambiguates Shield's two milestone-exit items in the next burndown (weeks 26–28). BigBrain signs.

---

_Audit drafted by Jury on 2026-05-12, Phase 9 V2 Batch 1, against `sprint/milestone-V2.md` exit gate + `PRD.md` §4 V2 + §7.3 + `BUILD_FLOW.md` Phase 9 + EU AI Act Art. 50. Self-dogfood gate V2 applied._

_Jury sign: 2026-05-12._
