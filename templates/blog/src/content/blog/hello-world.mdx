---
title: "Hello, world"
description: "First post on this blog. Replace me with the real first post."
pubDate: 2026-05-09
tags: ["meta"]
---

This is a sample MDX post. Edit `src/content/blog/hello-world.mdx` or add new files to that
directory — they're auto-discovered via the content collection in `src/content.config.ts`.

## Headings work

Body copy uses prose-friendly typography. You can mix in **markdown**, _italics_, and standard
formatting.

```ts
// Code blocks are syntax-highlighted via Shiki
function greet(name: string) {
  return `Hello, ${name}`;
}
```

## What this template enforces (per Stage 1.5 blog roster)
- LCP and CLS are the entire game — Astro static-first solves both
- SEO via the sitemap integration
- RSS feed at `/rss.xml`
- WCAG AA contrast on the default palette
